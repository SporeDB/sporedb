identity: bob
keyring: bob.pem

db:
  path: .db_bob
  driver: rocksdb
  policies:
    - policy.json

mycelium:
  listen: "localhost:4110"
  peers:
    - "localhost:4010"

api:
  listen: "localhost:4100"
